name: Deploy to Aliyun

# 触发条件
on:
  push:
    branches:
      - main # 当推送到 main 分支时触发
  workflow_dispatch: # 允许在 GitHub Actions 页面手动触发

# 任务定义
jobs:
  deploy:
    name: 🚀 部署到阿里云服务器
    runs-on: ubuntu-latest # 在 GitHub 提供的 Ubuntu 虚拟机上运行

    steps:
      # ==================== 步骤 1: 检出代码 ====================
      # 作用: 将仓库代码下载到 GitHub Runner
      - name: 📥 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 获取完整的 Git 历史

      # ==================== 步骤 2: 设置 Node.js 环境 ====================
      # 作用: 安装指定版本的 Node.js
      - name: 🟢 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # ==================== 步骤 3: 安装 pnpm ====================
      # 作用: 安装 pnpm 包管理器
      - name: 📦 安装 pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      # ==================== 步骤 4: 安装依赖 ====================
      # 作用: 根据 pnpm-lock.yaml 安装项目依赖
      - name: 📦 安装依赖
        run: pnpm install --frozen-lockfile

      # ==================== 步骤 5: 代码质量检查 ====================
      # 作用: 运行 ESLint 检查代码质量，如果失败则停止部署
      - name: 🔍 代码质量检查
        run: pnpm run lint
        continue-on-error: false # 失败则停止后续步骤

      # ==================== 步骤 6: 构建项目 ====================
      # 作用: 构建生产版本（生成 dist 目录）
      - name: 🔨 构建项目
        run: pnpm run build
        env:
          NODE_ENV: production

      # ==================== 步骤 7: 部署到服务器 ====================
      # 作用: 通过 SSH 连接到服务器，执行部署脚本
      - name: 🚀 部署到服务器
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          script: |
            echo "连接到服务器成功！"
            echo "开始执行部署脚本..."

            cd ${{ secrets.DEPLOY_PATH }}
            bash scripts/deploy.sh

            echo "部署脚本执行完成！"

      # ==================== 步骤 8: 部署结果通知 ====================
      # 作用: 无论成功失败都执行，输出部署结果
      - name: 📢 部署结果通知
        if: always() # 无论成功失败都执行
        run: |
          if [ ${{ job.status }} == 'success' ]; then
            echo "✅ 部署成功！"
            echo "🌐 访问地址: http://${{ secrets.SERVER_HOST }}"
          else
            echo "❌ 部署失败！请检查日志"
          fi
