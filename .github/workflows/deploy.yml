name: Deploy to Aliyun

# 触发条件
on:
  push:
    branches:
      - main # 当推送到 main 分支时触发
  workflow_dispatch: # 允许在 GitHub Actions 页面手动触发

# 任务定义
jobs:
  deploy:
    name: 🚀 部署到阿里云服务器
    runs-on: ubuntu-latest # 在 GitHub 提供的 Ubuntu 虚拟机上运行

    steps:
      # ==================== 步骤 1: 检出代码 ====================
      - name: 📥 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ==================== 步骤 2: 设置 Node.js 环境 ====================
      - name: 🟢 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # ==================== 步骤 3: 安装 pnpm ====================
      - name: 📦 安装 pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      # ==================== 步骤 4: 安装依赖 ====================
      - name: 📦 安装依赖
        run: pnpm install --frozen-lockfile

      # ==================== 步骤 5: 代码质量检查 ====================
      - name: 🔍 代码质量检查
        run: pnpm run lint
        continue-on-error: false

      # ==================== 步骤 6: 构建项目 ====================
      - name: 🔨 构建项目
        run: pnpm run build
        env:
          NODE_ENV: production
          VITE_CDN_URL: https://${{ secrets.CDN_DOMAIN }}/ # 全站 CDN URL

      # ==================== 步骤 7: 上传到阿里云 OSS (全站 CDN) ====================
      - name: 📤 上传全站资源到阿里云 OSS
        env:
          ALIYUN_ACCESS_KEY_ID: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
          ALIYUN_ACCESS_KEY_SECRET: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}
          ALIYUN_OSS_BUCKET: ${{ secrets.ALIYUN_OSS_BUCKET }}
          ALIYUN_OSS_REGION: ${{ secrets.ALIYUN_OSS_REGION }}
        run: |
          echo "📦 安装 ossutil..."
          # 下载 ossutil (Linux AMD64)
          wget -q https://gosspublic.alicdn.com/ossutil/install.sh
          sudo bash install.sh

          echo "⚙️  配置 ossutil..."
          # 配置 ossutil
          ossutil config -e ${ALIYUN_OSS_REGION}.aliyuncs.com \
            -i ${ALIYUN_ACCESS_KEY_ID} \
            -k ${ALIYUN_ACCESS_KEY_SECRET} \
            -L CH \
            -c /home/runner/.ossutilconfig

          echo "📤 上传全站资源到 OSS..."
          # 上传整个 dist 目录（增量更新）
          ossutil cp -r dist/ oss://${ALIYUN_OSS_BUCKET}/ \
            --update \
            --config-file /home/runner/.ossutilconfig

          echo "✅ 全站资源已上传到 OSS"
          echo "🌐 CDN 访问地址: https://cdn.slyai.top/"

      # 全站 CDN 模式：资源已全部上传到 OSS，无需上传到服务器
      # 如需保留服务器作为备用，可取消注释以下步骤

      # ==================== 步骤 8: 部署结果通知 ====================
      - name: 📢 部署结果通知
        if: always()
        run: |
          if [ ${{ job.status }} == 'success' ]; then
            echo "✅ 全站 CDN 部署成功！"
            echo "🌐 CDN 访问地址: https://${{ secrets.CDN_DOMAIN }}/"
            echo "📄 首页: https://${{ secrets.CDN_DOMAIN }}/index.html"
            echo ""
            echo "💡 提示: 如需刷新 CDN 缓存，请访问阿里云 CDN 控制台"
          else
            echo "❌ 部署失败！请检查日志"
          fi
